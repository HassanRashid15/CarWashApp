/**
 * Test setup file
 * Runs before all tests
 */

// Mock environment variables
// Note: NODE_ENV is read-only, but vitest sets it automatically

// You can add global test setup here
// For example, mock Supabase, external APIs, etc.

